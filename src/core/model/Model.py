from abc import ABC, abstractmethodclass Model(ABC,object):    @abstractmethod    def rules(self):        pass    def validate(self):        for attr, handler, msg in self.rules():            value = self            for subAttr in attr.split("."):                value = getattr(value,subAttr)            if not handler(value):                return False, msg        return True,None